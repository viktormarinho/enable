{
  "name": "enable",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cargo build && pnpm --filter client build && pnpm --filter @enable/ts-sdk build"
  },
  "author": "viktormarinho"
}
